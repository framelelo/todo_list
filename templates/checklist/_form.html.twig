{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="my-2 text-center">
        <button class="btn my-3">{{ button_label|default('SAUVEGARDE') }}</button>
    </div>
{{ form_end(form) }}

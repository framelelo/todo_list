{% extends 'base.html.twig' %}

{% block title %}Modifier ma checklist{% endblock %}

{% block body %}

<div class="container my-5 checklist">
        <div class="top_container mb-3">
            <h1> {{ checklist.name }}</h1>

            <a class='return-btn my-3' href="{{ path('app_checklist_index') }}"> <i class="fas fa-backward"></i> Retour</a>

        </div>

        {{ include('checklist/_form.html.twig', {'button_label': 'SAUVEGARDE'}) }}

    <div class="my-2 text-center">
        {{ include('checklist/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
